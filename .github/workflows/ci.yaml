name: Release CI

on:
  push:
    branches: ["main"]
    tags: ["v*.*.*"]

jobs:
  deploy:
    uses: ./.github/workflows/deploy-reusable.yaml
    secrets: inherit
    with:
      project-name: mmw
      stage: ${{ vars.STAGE }}
      fd-aws-tf-modules-version: v2.23.0
